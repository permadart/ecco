name: ecco
description: A simple, MVVM-focused state management solution for Flutter.
version: 0.0.1+2
homepage: https://permad.art/ecco

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
